{
  "name": "workpay-android",
  "version": "1.0.0",
  "private": true,
  "description": "WorkPay wrapped as a native Android app for continuous background geofencing",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor-community/background-geolocation": "^1.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
